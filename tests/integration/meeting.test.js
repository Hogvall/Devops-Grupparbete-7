import { describe, test, expect, beforeEach } from "vitest";

describe("Description", () => {
  
  beforeEach(() => {
   
  });

  test("Test description", () => {
    expect(1).toEqual(1);
  });
  
});