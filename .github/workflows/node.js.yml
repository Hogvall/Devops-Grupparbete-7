name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint-and-tests:
    runs-on: ubuntu-latest
    env:
      VITE_SUBABASE_KEY: ${{ secrets.VITE_SUBABASE_KEY }}

    strategy:
      matrix:
        node-version: [22, 24]

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Build project
        run: npm run build

      - name: Run lint
        run: npm run lint

      - name: Debug env
        run: |
          echo "Key exists: ${{ env.VITE_SUBABASE_KEY != '' }}"

      - name: Debug raw
        run: echo "Value: $VITE_SUPABASE_KEY"

      - name: Run tests
        run: npm run test